{
    "require": {
        "php": ">=7.4",
        "ext-pdo": "*"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^11.5",
        "squizlabs/php_codesniffer": "^3.11"
    },
    "scripts": {
        "test": "php vendor/bin/phpunit",
        "phpstan": "php vendor/bin/phpstan analyse -c phpstan.neon --memory-limit=1024M",
        "pcf": "php vendor/bin/phpcs -d memory_limit=1024M --ignore=vendor/ --extensions=php,css .",
        "pcbf": "php vendor/bin/phpcbf -d memory_limit=1024M --ignore=vendor/ --extensions=php,css ."
    },
    "autoload": {
        "psr-4": {
            "Ruubik\\": "src/"
        }
    }
}
